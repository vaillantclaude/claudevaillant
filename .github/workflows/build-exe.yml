  UW PICO 5.09         File: .github/workflows/build-exe.yml          Modified  

/anonymisation/anonymisation.py 

path: dist/anonymisation.exe
name: Build Windows EXE

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
  
jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"
        
    - name: Install dependencies
      run: |
        pip install pyinstaller
        
    - name: Build EXE
      run: |
        pyinstaller --onefile anonymisation/anonymisation.py
        
    - name: Upload EXE as artifact
      uses: actions/upload-artifact@v3
      with:
        name: anonymisation-exe
      
name: Build Windows EXE

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        pip install pyinstaller

    - name: Build EXE
      run: |
        pyinstaller --onefile anonymisation/anonymisation.py

    - name: Upload EXE as artifact
      uses: actions/upload-artifact@v3
      with:
        name: anonymisation-exe
        path: dist/anonymisation.exe
git commit -m "Clean workflow file"
git push
name: Build Windows EXE

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        pip install pyinstaller

    - name: Build EXE
      run: |
        pyinstaller --onefile anonymisation/anonymisation.py

    - name: Upload EXE as artifact
      uses: actions/upload-artifact@v3
      with:
        name: anonymisation-exe
        path: dist/anonymisation.exe
git add .github/workflows/build-exe.yml
git commit -m "Clean workflow file"
git push

nano .github/workflows/build-exe.yml

name: Build Windows EXE

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        pip install pyinstaller

    - name: Build EXE
      run: |
        pyinstaller --onefile anonymisation/anonymisation.py

    - name: Upload EXE as artifact
      uses: actions/upload-artifact@v3
      with:
        name: anonymisation-exe
        path: dist/anonymisation.exe

